// Definieer een constante voor de API-URL waar de parkeerdata wordt opgehaald.
// https://data.stad.gent/api/explore/v2.1/catalog/datasets/bezetting-parkeergarages-real-time/records

// Definieer een constante om de spinner te kunnen selecteren adhv jquery

// Definieer een constante om de container te selecteren (jquery) waarin alle parking items geplaatst zullen worden

/** 
 * Schrijf een function om data op te halen via de API
 * function fetchData() {}
 * 
 * Gebruik ajax om de requests te versturen
 * Verwijder bestaande html parking elementen alvorens nieuwe data op te halen
 * Toon de spinner tijdens het laden van data
 * Verberg de spinner na het laden
 * Update parking info html
 */ 

/**
 * Schrijf een functie die de opgehaalde parkeergegevens weergeeft
 * function updateParkingInfo(parkingData) {}
 * 
 * Sorteer de gegevens op naam (alfabetisch)
 * 
 * Voeg voor elk item in de opgehaalde data een nieuw infobord toe
 */

/**
 * Schrijf een functie die een HTML-element maakt voor een parkeerinformatie bord
 * Function generateParkingTemplate(parking) {}
 */

// Voer de functie fetchData() uit van zodra de pagina geladen is
